# Core dependencies (required on all platforms)
PyPDF2>=3.0.0
python-docx>=1.0.0
pdfplumber>=0.10.0

# Optional: NLP support (can be skipped if not needed)
# spacy>=3.0.0

# macOS-specific dependencies are in requirements-macos.txt
# Install with: pip install -r requirements-macos.txt (macOS only)

